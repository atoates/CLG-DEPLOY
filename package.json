{
  "name": "crypto-lifeguard-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node migrate.js && node update-tags.js && node server.js",
    "preview": "vite preview --host",
    "migrate": "node migrate.js",
    "backup": "node backup.js",
    "update-tags": "node update-tags.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vite": "^5.4.20"
  },
  "engines": {
    "node": ">=18"
  }
}
