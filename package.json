{
  "name": "crypto-lifeguard",
  "version": "1.0.0",
  "description": "Crypto Lifeguard - Main App",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "install:all": "npm install",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules"
  },
  "dependencies": {
    "serve": "^14.2.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.4.20"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
