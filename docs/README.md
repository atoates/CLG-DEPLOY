# Crypto Lifeguard 🏊‍♂️# Crypto Lifeguard — In-Browser Demo



Monorepo for Crypto Lifeguard platform - Admin Dashboard and Backend APIA zero-dependency web prototype you can open in any modern browser. It lets users:



## 📁 Structure- Search and **select tokens** to watch

- See **dummy alerts** with severity icons (🚨 / ⚠️ / 🛟) and **live countdowns**

```- Read a **mock AI weekly summary** tailored to selected tokens

crypto-lifeguard/

├── apps/## How to run

│   ├── admin/          # React admin panel

│   └── backend/        # Node.js API server1. Download the three files in this folder:

├── packages/   - `index.html`

│   └── shared/         # Shared TypeScript types   - `styles.css`

└── package.json        # Root workspace config   - `app.js`

```2. Open `index.html` directly in your browser (double-click it). No build or server needed.



## 🚀 Quick Start## Notes



### Install Dependencies- This is a front-end only demo. All data is hardcoded and stored in memory (with localStorage for selected tokens).

- Colours follow the brand palette (Ocean Blue, Lifeguard Yellow, Coral Red, Sandy Beige).

```bash- You can type any token symbol (e.g., `BTC`, `ETH`, `MATIC`). Unknown symbols that look valid (A–Z/0–9) are allowed for demo.

npm install- The weekly summary is mocked; in a full app this would be generated by an LLM from real data.

```

### Auth (optional)

This will install dependencies for all workspaces.- Set environment variables to enable Google SSO (demo implementation):

   - `BASE_URL` (e.g., `http://localhost:3000` for local)

### Development   - `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`

- Visit `/signup` to start; `/profile` shows current user and prefs.

```bash// Trigger deployment Thu Oct  2 12:53:35 BST 2025

# Run both admin and backend in development mode

npm run dev


# Or run individually:
npm run dev:admin    # Admin panel on http://localhost:5173
npm run dev:backend  # Backend API on http://localhost:3000
```

### Build

```bash
# Build admin panel
npm run build

# Or specifically:
npm run build:admin
```

## 📦 Workspaces

### apps/admin

React + TypeScript + Vite admin dashboard for managing alerts, news, and tokens.

**Tech Stack:**
- React 18
- TypeScript
- Vite
- TanStack Query
- TailwindCSS

**Key Features:**
- Alert management
- News feed management
- AI-powered alert generation
- Token request management

**Scripts:**
```bash
cd apps/admin
npm run dev      # Start dev server
npm run build    # Build for production
npm run preview  # Preview production build
```

### apps/backend

Node.js + Express API server with PostgreSQL database.

**Tech Stack:**
- Node.js
- Express
- PostgreSQL
- News APIs (CoinDesk, CryptoNews, etc.)

**Key Features:**
- RESTful API endpoints
- Database migrations
- News aggregation
- Alert management
- Admin authentication

**Scripts:**
```bash
cd apps/backend
npm start           # Start server
npm run migrate     # Run database migrations
```

### packages/shared

Shared TypeScript types and utilities used across admin and backend.

## 🗄️ Database

PostgreSQL database hosted on Railway.

**Migrations:**
```bash
npm run migrate --workspace=apps/backend
```

## 🌐 Deployment

### Railway Configuration

Both apps deploy from this monorepo but as separate services:

**Backend Service (CLG-DEPLOY):**
- Root Directory: `apps/backend`
- Build Command: `cd apps/backend && npm install`
- Start Command: `cd apps/backend && npm start`
- Environment Variables: See `apps/backend/.env.example`

**Admin Service (CLG-ADMIN):**
- Root Directory: `apps/admin`
- Build Command: `cd apps/admin && npm install && npm run build`
- Start Command: `cd apps/admin && npm run preview`
- Environment Variables: See `apps/admin/.env.example`

### URLs

- **Production API:** https://app.crypto-lifeguard.com
- **Admin Panel:** https://clg-admin-production.up.railway.app
- **Staging API:** https://clg-staging.up.railway.app

## 🔧 Development Workflow

### Adding a New Feature

1. **Create shared types** (if needed):
   ```bash
   # Edit packages/shared/types.ts
   ```

2. **Backend changes**:
   ```bash
   cd apps/backend
   # Add API endpoints, database migrations, etc.
   ```

3. **Frontend changes**:
   ```bash
   cd apps/admin
   # Add UI components, API calls, etc.
   ```

4. **Test locally**:
   ```bash
   npm run dev  # From root
   ```

5. **Commit and push**:
   ```bash
   git add .
   git commit -m "feat: your feature description"
   git push origin main
   ```

Railway will automatically deploy both services.

## 📝 Environment Variables

### Backend (`apps/backend/.env`)

```env
DATABASE_URL=postgresql://...
ADMIN_TOKEN=your-admin-token
ADMIN_DASHBOARD_URL=https://clg-admin-production.up.railway.app
OPENAI_API_KEY=sk-...
# See apps/backend/.env.example for full list
```

### Admin (`apps/admin/.env`)

```env
VITE_API_URL=https://app.crypto-lifeguard.com
VITE_OPENAI_API_KEY=sk-...
```

## 🧪 Testing

```bash
# Backend tests
cd apps/backend
npm test

# Admin tests
cd apps/admin
npm test
```

## 📚 Documentation

- [Backend API Documentation](./apps/backend/README.md)
- [Admin Panel Guide](./apps/admin/README.md)
- [Token Request System](./TOKEN_REQUEST_SYSTEM.md)
- [Architecture Refactor Proposal](./ARCHITECTURE_REFACTOR_PROPOSAL.md)

## 🤝 Contributing

1. Create a feature branch
2. Make your changes
3. Test locally
4. Submit a pull request

## 📄 License

Private - All Rights Reserved

---

**Built with ❤️ for the crypto community**
